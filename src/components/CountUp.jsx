// CountUp component removed — kept as a no-op placeholder to avoid breaking imports
// This component was unused in the app. If you want it fully removed from the repo,
// delete this file. For now it returns null so any accidental imports won't break.
// REMOVED: CountUp component was unused and cleaned up.
// If you need the original implementation, restore it from version control.
export default function CountUp() {
  return null;
}
